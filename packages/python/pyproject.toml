[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "rim-ui-mcp"
version = "1.0.0"
description = "RimWorld state exploration and real GUI automation"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "JerryLunar (Hcrab)" }]
